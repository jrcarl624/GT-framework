/*Different files are not required but can be useful for separating your code; making it more understandable, make sure to export.*/
function exampleFunction(inp) {
    console.warn('This is from exampleModule' + inp)
}

export { exampleFunction }
